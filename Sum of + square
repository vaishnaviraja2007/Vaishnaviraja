#include <stdio.h>

int main() {
    int n, i, value;
    int sum = 0;

    scanf("%d", &n);

    for (i = 0; i < n; i++) {
        scanf("%d", &value);
        if (value > 0) {
            sum += value * value;
        }
    }

    printf("%d\n", sum);

    return 0;
}

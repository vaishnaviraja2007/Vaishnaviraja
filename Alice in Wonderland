#include<stdio.h>

int main()
{
    int number;

    scanf("%d",&number);

    int sum_of_digits = (number / 10) + (number % 10);

    printf("Bird said:%d\n", number);

    printf("Alice must go in path-%d\n", sum_of_digits);

    return 0;
}


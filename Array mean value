#include <stdio.h>

int main() {

    int n, i, value;

    int sum = 0;

    float mean;

    scanf("%d", &n);

    for (i = 0; i < n; i++) {

        scanf("%d", &value);

        sum += value;

    }

    mean = (float)sum / n;

    printf("Array Mean Value is %.2f\n", mean);

    return 0;

}
